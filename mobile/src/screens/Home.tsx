import React, { FC } from 'react';
import { View, Text } from 'react-native';

interface HomeProps {}

const Home: FC<HomeProps> = () => {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
