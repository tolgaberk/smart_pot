import React, { FC } from 'react';
import { View, Text } from 'react-native';

interface FlowersListProps {}

const FlowersList: FC<FlowersListProps> = () => {
  return (
    <View>
      <Text>FlowersList</Text>
    </View>
  );
};

export default FlowersList;
