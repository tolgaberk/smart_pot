import { StyleSheet } from 'react-native';

const generalStyles = StyleSheet.create({
  flex: {
    flex: 1,
  },
  centerView: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  justiftCenter: { justifyContent: 'center' },
  alignCenter: { alignItems: 'center' },
  debug: { borderWidth: 2 },
});
export default generalStyles;
